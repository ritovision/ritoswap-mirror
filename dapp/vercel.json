{
  "framework": "nextjs",
  "buildCommand": "pnpm prisma generate --no-engine && pnpm next build && pnpm generate:openapi",
  "installCommand": "echo 'Skip - done in CI'",
  "outputDirectory": ".next"
}
